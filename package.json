{
  "name": "benmiriello2024",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "start-dev": "concurrently \"cd server && npm run start-dev\" \"cd client && npm start\"",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "v20.11.1",
    "npm": "10.2.4"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
